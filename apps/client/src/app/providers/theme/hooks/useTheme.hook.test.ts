// Intentionally empty module to avoid duplicate JSX parsing during test runs.
import { describe } from 'vitest';

describe.skip('placeholder duplicate test file', () => {
	// Intentionally skipped - real tests live in the .tsx file
});
